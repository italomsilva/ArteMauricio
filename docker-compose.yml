version: '3.7'

services:
  backend:
    build: ./backend
    ports:
      - "3012:3001" 
    environment:
      - NODE_ENV=production

  frontend:
    build: ./frontend
    ports:
      - "3015:3000" 
    environment:
      - NODE_ENV=production
