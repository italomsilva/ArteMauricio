'use client'
import ContactPage from "@/app/pages/contact_page/ContactPage" 

export default function Page(){
    return(
        <main style={{display: 'flex', blockSize:'100vh',alignItems: 'center', justifyContent: "center"}}>
            <ContactPage/>
        </main>
    )
}